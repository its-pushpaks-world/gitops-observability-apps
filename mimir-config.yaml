multitenancy_enabled: true

common:
  storage:
    backend: local
    local:
      directory: /tmp/mimir

blocks_storage:
  backend: local
  local:
    directory: /tmp/mimir/blocks

compactor:
  data_dir: /tmp/mimir/compactor
